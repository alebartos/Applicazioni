# ============================================
# Backend Environment Variables
# Digital Messaging Game - Secure Configuration
# ============================================

# Database URL (SQLite file path)
DATABASE_URL="file:./data/messaging-game.db"

# Server Port
PORT=3001

# ============================================
# SECURITY CONFIGURATION (REQUIRED)
# ============================================

# JWT Secret Key - MUST be changed in production!
# Generate a strong random key: openssl rand -base64 32
JWT_SECRET="CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_IN_PRODUCTION"

# Admin Secret for Legacy Login (Optional - can be removed after migration to JWT)
ADMIN_SECRET="MESSAGINGAME2025!ADMIN"

# ============================================
# CORS Configuration
# ============================================

# Allowed origins for CORS (comma-separated list)
# Development default: http://localhost:5173,http://localhost:3000
# Production: Set to your actual frontend domain
CORS_ORIGIN="http://localhost:5173,http://localhost:3000"

# Production example:
# CORS_ORIGIN="https://your-domain.com,https://www.your-domain.com"

# ============================================
# Production Deployment Notes
# ============================================

# 1. ALWAYS change JWT_SECRET to a strong random value
# 2. ALWAYS set CORS_ORIGIN to your actual frontend domain
# 3. Use HTTPS in production (configured at reverse proxy level)
# 4. Consider setting different JWT_SECRET for different environments
# 5. Never commit the actual .env file to version control

# ============================================
# Optional: Node Environment
# ============================================
NODE_ENV="development"
# NODE_ENV="production"
